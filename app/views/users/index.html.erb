<%= provide(:title,"All users") %>
<h1>All users</h1>
<ul class="users">
  <% @users.each do |user| %>
  <li>
    <a href="<%= user_path(user) %>">
      <%= gravatar_for(user) %>
    </a>
    <%= link_to user.name, user %>
  </li>
  <% end %>
</ul>
</div>
